version: 1

formatters:

  simple:
    format: "%(asctime)s - %(levelname)s - %(message)s"

handlers:

  file:
    class: logging.FileHandler
    filename: app.log
    formatter: simple
    level: ERROR

root:
  level: ERROR
  handlers:
    - file